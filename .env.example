DATABASE_URL=postgresql://user:password@host/database?sslmode=require
GEMINI_MASTER_KEY=replace-with-a-long-random-secret
STUDIO_USERNAME=ahmed
STUDIO_PASSWORD=replace-with-a-strong-password
# Optional: configure the Gemini key here instead of saving it from the UI.
# GEMINI_API_KEY=
